# 画面だけあって未実装のもの一覧

## 対応済み（High優先度）

- ~~業界別モード4枠のフィルタリング~~ → `store.startIndustry(code)` で実際にその業界の3問だけ出題されるようにした
- ~~業界別モードの「3/10」進捗表示~~ → 「全3問」という正確な表記に修正
- ~~「業界別モード」見出し横の「すべて見る」リンク~~ → 削除（4業界すべて表示済みで遷移先が無いため）
- ~~復習リストの「もう一度」ボタン~~ → 削除（クリックハンドラが無かったため）
- ~~ホームの「全4問」表示~~ → 実際の問題数（`QUESTIONS.length`）に連動する表示に修正

## 対応済み（Medium優先度）

- ~~「12日連続」ストリーク表示~~ → `ProfileStore`（localStorage永続化）が実際の連続プレイ日数を管理
- ~~「読解レベル4・財務の目 620/900pt」の進捗バー~~ → 実際の累計ポイントに応じてレベル・進捗が上がる
- ~~「連続12日」チップ~~ → 実データ（`profile.streakDays`）に接続
- ~~「+40 XP」チップ~~ → 削除し、代わりに実際にプレイした難易度チップとレベルアップ時のみの通知に置き換え

## 未対応（Low）

### 結果画面

- **「結果をシェア」ボタン** — クリックハンドラなし、共有機能は未実装
- **「次の1問は明日の朝6時に更新」の文言** — 日替わり出題ロジックは未実装（`StaticQuizRepository.getDailyQuestions(date)` も `date` 引数を無視して常に同じ12問を返すだけ）

### 共通基盤

- **ユーザーアカウント／ログイン** — 画面自体が存在しない（スコープ外として合意済み）
